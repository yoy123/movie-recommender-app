<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />

    <application
        android:banner="@drawable/ic_tv_banner">
        <!--
            TV launchers typically prefer LEANBACK_LAUNCHER and may ignore android:banner
            when the app also declares a standard LAUNCHER entry point.
        -->
        <activity
            android:name="com.movierecommender.app.firestick.MainActivity"
            android:banner="@drawable/ic_tv_banner"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- Remove the phone/tablet launcher entry point inherited from main/manifest. -->
            <intent-filter tools:node="removeAll">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
