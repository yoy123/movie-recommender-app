# Project-wide Gradle settings.
# IDE (Android Studio and Intellij)
android.useAndroidX=true
android.enableJetifier=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Enable Gradle build cache
org.gradle.caching=true

# Configure JVM memory
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Disable VS Code/Eclipse integration that causes conflicts
org.gradle.java.installations.auto-detection=false

# Release signing configuration
RELEASE_STORE_FILE=app/signing/release.jks
RELEASE_STORE_PASSWORD=movierecommender2024
RELEASE_KEY_ALIAS=release
RELEASE_KEY_PASSWORD=movierecommender2024
